export type ProductCategoryType = {
  id: number;
  category: string;
  img_url: string | null;
  description: 'dd';
  tag_id: 1;
};

export type ProductCategoriesArrayType = Array<ProductCategoryType>;
